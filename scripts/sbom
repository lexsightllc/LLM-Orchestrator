#!/usr/bin/env bash
# SPDX-License-Identifier: MPL-2.0
set -euo pipefail

source "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/_common.sh"

mkdir -p "${ROOT_DIR}/sbom"
run_python -m cyclonedx_py --environment pip --json --output "${ROOT_DIR}/sbom/sbom.json"
